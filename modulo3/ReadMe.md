Execute o program em main.py

ops é a classe que gera o objeto do produto

Em iputs, entradas.checar_mensagem() quando a resposta for == 'add' ele pegara os atributos para istanciar um objeto, e retornará uma lista com as demais informações do produto 